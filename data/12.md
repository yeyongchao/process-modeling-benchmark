# Example 9.4



## Input

For the nonisothermal batch reactor with first order parallel reactions $A \rightarrow B$, $A \rightarrow C$, we maximize the final amount of product $B$ subject to an upper temperature bound. The optimal control problem over $t \in $ can be stated as

## Output

### 1. Variables

$a(t)$: Concentration of reactant $A$ at time $t$

$b(t)$: Concentration of product $B$ at time $t$

$u(t)$: Control profile (transformed temperature) at time $t$

### 2. Objective Function

Maximize the final amount of product $B$ at $t=1$, formulated as minimizing its negative:

$$\min -b(1)$$

### 3. Constraints

Process Model (Differential Equations):

$$\frac{da}{dt} = -a(t)(u(t) + ku(t)^\beta)$$

$$\frac{db}{dt} = a(t)u(t)$$

Initial Conditions:

$$a(0) = 1$$

$$b(0) = 0$$

Control Variable Bounds:

$$u(t) \in [0, U]$$